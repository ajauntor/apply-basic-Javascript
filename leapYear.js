function isLeapYear(leapYear) {
    const remainder = leapYear % 4;
    if (remainder == 0) {
        return true;
    }
    else {
        return false;
    }
}

function isLeapYear(leapYear) {
    const remainder = leapYear % 4;
    if (remainder == 0) {
        return true;
    }
    else {
        return false;
    }
}